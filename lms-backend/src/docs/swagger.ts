import { Application } from 'express';

export const setupSwagger = (app: Application) => {
  // Aqui futuramente você colocará a configuração real do Swagger
  console.log('Documentação Swagger pronta para ser configurada.');
};